// cell will always be a square -> width and height will be always the same number
const sizeOfCell = "50";

const myTheme = {
  widthOfCell: sizeOfCell + "px",
  heightOfCell: sizeOfCell + "px",
  hoverColor: "#d4c7c7",
};

export default myTheme;
